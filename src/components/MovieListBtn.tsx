
import { Button } from "@chakra-ui/react";
import { FiPlus } from "react-icons/fi";

export default function MovieListBtn() {
  return (
    <Button color="white" colorScheme="white" bg="" leftIcon={<FiPlus />}>
      My List
    </Button>
  );
}
